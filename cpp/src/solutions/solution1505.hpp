#pragma once
#include <string>

namespace solutions::solution1505
{
    bool is_nice(const std::string &str);
    bool is_nice_2(const std::string &str);
    bool contains_three_vowels(const std::string &str);
    bool letter_twice(const std::string &str);
    bool contains_blacklisted_strings(const std::string &str);
    bool contains_pair_twice(const std::string &str);
    bool contains_letter_between_pair(const std::string &str);
}
